# flask-top-movies-app
An application developed using Flask in Python which keeps a track of reviews, ratings, description, release year of movies. The data is fetched from TMDB API for a specific movie searched by the user. The user is able to edit/add/delete movies from this list. Data is stored in SQLite database.
